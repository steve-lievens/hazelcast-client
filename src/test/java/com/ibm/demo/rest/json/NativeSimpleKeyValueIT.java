package com.ibm.demo.rest.json;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeSimpleKeyValueIT extends SimpleKeyValueTest {

    // Execute the same tests but in native mode.
}